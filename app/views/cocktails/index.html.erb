<div class="banner" style="background-image: opacity: 0.5, url('<%= image_path 'bubbles.png' %>');">

  <div class="banner-h1">
    <h1>PICK YOUR COCKTAIL</h1>
  </div>

    <div class="container">
      <div class="my-4">
        <div class="row">
          <%= render partial: 'cocktails/card', collection: @cocktails, as: :cocktail %>
        </div>
      </div>
    </div>
    <%= link_to "Add a new cocktail", new_cocktail_path, class: 'btn btn-primary' %>
</div>
